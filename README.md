This is a python Pomodoro Timer gui implemented using:
Tkinter for the GUI.
It uses a timer to break work into intervals, traditionally 25 minutes long, separated by short breaks then followed by long breaks. 
